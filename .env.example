# Application
NODE_ENV=development
PORT=3000

# Database - Credentials will be provided by Docker Compose
DB_HOST=localhost # App defaults to this, but Docker will override
DB_PORT=5432
DB_USERNAME=weconnect
DB_PASSWORD=weconnect123
DB_DATABASE=weconnect

# JWT
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# Redis
REDIS_HOST=localhost # App defaults to this, but Docker will override
REDIS_PORT=6379

# Encryption
ENCRYPTION_KEY=your-encryption-key-32-chars-long